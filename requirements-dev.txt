# Development / test-only dependencies.
# Not included in production images.
# Install: venv/Scripts/pip install -r requirements-dev.txt
#
# Test framework
pytest==8.3.3
pytest-asyncio==0.24.0

# BGE-M3 로컬 임베딩 (희영 연구용 — GPU+fp16 활성화, 로컬 테스트 전용)
# 2026-04-23: NeMo + coqui-tts 도입으로 의존성 버전 변경됨
# chromadb 0.5.23 의 tokenizers 충돌 경고는 pip warning 수준이며 실동작 영향 없음
FlagEmbedding
tokenizers==0.22.2
transformers==4.57.1
