-- ==============================================================================
-- 00_extensions.sql — PostgreSQL 확장 기능
-- ==============================================================================
-- Postgres 13+ 에서 gen_random_uuid() 는 기본 제공되지만,
-- 명시적으로 pgcrypto 확장을 활성화하여 버전 차이에 안전하게 대응합니다.
-- ==============================================================================

CREATE EXTENSION IF NOT EXISTS pgcrypto;
