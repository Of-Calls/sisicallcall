# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1

# Twilio
twilio==9.3.2

# LangGraph / LangChain
langgraph==0.2.28
langchain==0.3.1
langchain-openai==0.2.1

# LLM
openai==1.57.0  # 1.52+ : httpx 0.28 proxies 호환 수정

# STT
deepgram-sdk==3.7.0

# TTS — Azure Speech SDK (μ-law 8kHz 네이티브 출력, Twilio 호환)
azure-cognitiveservices-speech==1.40.0

# Speaker Verification
# TitaNet (대영 R-01 채택) — NeMo ASR 포함
nemo-toolkit[asr]==2.7.2

# Embedding / Vector DB
chromadb==0.5.23

# Database
asyncpg==0.29.0

# Cache
redis[asyncio]==5.1.0

# Config
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Audio
numpy==2.4.4  # NeMo 2.7.2 설치로 1.26.4 → 2.4.4 업그레이드됨

# PyTorch + CUDA — cu124 인덱스에서 설치 필요
# pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# OCR
pytesseract==0.3.13
Pillow==10.4.0

# Face Auth (M3+) — ArcFace via insightface + ONNX Runtime (CPU)
insightface==0.7.3
onnxruntime==1.19.2
opencv-python-headless==4.10.0.84

# Async Worker (TBD: Celery or Redis Stream)
celery==5.4.0

# SMS
solapi==5.0.3

# tokenizers: chromadb<=0.20.3 요구 vs transformers>=0.22.0 요구 충돌
# → transformers(BGE-M3) 우선, chromadb는 런타임 정상 동작 확인
tokenizers==0.22.2

# HTTP Client
httpx==0.27.2

# Utils
python-multipart==0.0.12

# PDF Processing — pymupdf4llm: PDF → 마크다운 추출 (헤더/표 구조 보존)
pymupdf4llm
